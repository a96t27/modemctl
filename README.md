## Installation

1. Add `src-git modemctl https://github.com/a96t27/modemctl.git` to `feeds.conf` in build root.
2. `./scripts/feeds update -a`
3. `./scripts/feeds install -a -p myfeed`
4. `make menuconfig`

